  // let click = document.getElementById('click');
  let resetPage = document.getElementById('reset-page'); // Assuming there's only one element with the class 'reset-page'

  // Define the onClick function
  function onClick() {
      resetPage.classList.remove('display-box');
  }

  function onlick() {
      resetPage.classList.add('display-box');
  }